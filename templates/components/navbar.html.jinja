<nav class="bg-black p-4">
        <div class="max-w-7xl mx-auto px-4 flex items-center justify-between">
            <!-- Logo or Brand Name -->
            <a href="#" class="text-white text-2xl font-bold">Every Dice</a>

            <!-- Menu Links -->
            <div class="hidden md:flex space-x-6">
                <a href="/" class="text-white hover:text-gray-200">
                Home
                </a>
                
                <a href="/browse" class="text-white hover:text-gray-200">
                Shop
                </a>
                
                <a href="/cart" class="text-white hover:text-gray-200">
                Cart
                </a>

                <a href="#" class="text-white hover:text-gray-200">
                Contact
                </a>
                
                {% if not current_user.is_authenticated%}
                
                
                <a href="/login" class="text-white hover:text-gray-200">
                Login
                </a>
                
                {%else%}
                <div>
                    Hi, {{ current_user.name }}!

                    <a href="/logout" class="text-white hover:text-gray-200">Logout</a>
                </div>

                


                {% endif %}
            </div>
</nav>
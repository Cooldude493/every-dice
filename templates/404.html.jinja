<!doctype html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body class = "bg-blue-900 text-white min-h-screen">
  {% include "components/navbar.html.jinja"%}

      {% for message in get_flashed_messages()%}
        <ol class="bg-red-500 text-white font-bold rounded-t px-4 py-2 mt-4 mx-10">
         {{ message }}
        </ol>
    {% endfor%}

    
    
    <div class="bg-white text-blue-900 rounded-lg shadow-lg p-8 max-w-lg text-center mx-auto mt-30 ">
      <h1 class="text-4xl font-bold mb-4">404 Error</h1>
      <p class="text-lg mb-6">The page you are looking for does not exist.</p>
      <div class="flex justify-center space-x-4">
        <a href="/" class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded">
          Go to Homepage
        </a>
        <a href="/browse" class="bg-gray-500 hover:bg-gray-600 text-white font-bold py-2 px-4 rounded">
          Browse Pages
        </a>
      </div>
    </div>
  </body>
</html>